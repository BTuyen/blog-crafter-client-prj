# Tự động nạp khi chạy `next build` / `next start` (NODE_ENV=production) và khi deploy
# => môi trường production sẽ gọi backend production
NEXT_PUBLIC_API_BASE_URL=https://blog-crafter-backend.vercel.app
